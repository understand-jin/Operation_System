# 재고 시뮬레이션 페이지 설명서

> **파일 위치**: `pages/5_Inventory_Simulation.py`
> **목적**: 자사 기말재고 + 제조사 취소 PO를 통합하여 FEFO 방식으로 월별 재고 소진을 시뮬레이션하고, 부진재고 리스크를 분기별로 집계합니다.

---

## 전체 처리 흐름도

```mermaid
flowchart TD
    subgraph INPUT["1단계 · 입력 데이터"]
        A1["기말재고 Data\n자재 / 배치 / 유효기간\n기말재고 수량·금액"]
        A2["분류 및 원가율\n자재코드 / 대분류\n소분류 / 원가율"]
        A3["평판 기준\n자재 / 평판\n평판 × 1.38배"]
        A4["제조사 취소 현황\n제품코드 / 잔여 PO / 금액"]
    end

    subgraph NORMALIZE["2단계 · 자재코드 정규화"]
        B["normalize_code_to_int_string()\n숫자·문자·소수점 혼재 → 정수 문자열 통일\n예) '9310288.0' → '9310288'"]
    end

    subgraph MAPPING["3단계 · 데이터 매핑 (left join)"]
        C1["build_mapped_inventory_df()\n자사 기말재고\n+ 분류·원가율 매핑\n+ 평판 매핑\n→ 단가·출하원가·출하판가·판가 계산"]
        C2["build_mapped_cancel_po_df()\n제조사 취소 PO\n+ 분류·원가율 매핑\n+ 평판 매핑\n→ 단가·출하원가·출하판가 계산"]
    end

    subgraph REPORT1["4단계 · 대분류 소계 리포트"]
        D["build_major_only_report_table()\n자사 + 제조사 통합\n대분류별 소계 / 총계\n원가·판가 기준"]
    end

    subgraph COMBINE["5단계 · 데이터 통합 (자사 + 제조사)"]
        E1["combined_plain\n자사(plain) + 제조사(plain)"]
        E2["combined_x138\n자사(×1.38배) + 제조사(×1.38배)"]
    end

    subgraph FEFO["6단계 · FEFO 시뮬레이션"]
        F["simulate_monthly_remaining_amount_fefo()\n\n① 유효기간 오름차순으로 배치 정렬\n② 매달 출하원가(평판 기반)만큼 앞 배치부터 차감\n③ 유효기한 D-6개월 이후는 판매 중단\n④ 시즌 아이템 → 5·6·7·8월만 소진\n\n→ 월별 배치별 잔액 컬럼 생성\n   예) 26_1, 26_2, ..., 28_12"]
    end

    subgraph OBSOLETE["7단계 · 부진재고 분석"]
        G["add_obsolete_cols_at_cutoff_6m()\n\nD-6개월 시점의 잔액 = 부진재고량\n부진재고 진입 분기 라벨 생성\n예) '26년 3Q'\n회전월 계산 = 원가 / 출하원가"]
    end

    subgraph QUARTER["8단계 · 분기 집계표"]
        H["build_category_quarter_table()\n\n대분류·소분류별 KPI\n  - 원가 / 출하원가 / 출하판가 / 회전월\n분기별 부진재고 진입 금액 피벗\n총계 + 대분류 소계 포함"]
    end

    subgraph MERGE["9단계 · 최종 보고서 병합"]
        I["attach_cat_table()\n\n대분류 소계 리포트 (원가·판가)\n+ 자사+제조사 plain 분기 집계\n+ 자사+제조사 ×1.38배 분기 집계\n+ 자사만 plain 분기 집계\n+ 자사만 ×1.38배 분기 집계\n→ 가로로 이어 붙임"]
    end

    subgraph OUTPUT["10단계 · 출력"]
        J1["매핑 결과 샘플 데이터프레임"]
        J2["대분류 소계 통합 리포트"]
        J3["시뮬레이션 결과 요약 (상위 30행)"]
        J4["분기 집계표"]
        J5["최종 보고서 (단위: 억원)\n엑셀 다운로드"]
    end

    A1 & A2 & A3 --> NORMALIZE
    A4 --> NORMALIZE
    NORMALIZE --> MAPPING
    A2 & A3 --> MAPPING

    C1 --> REPORT1
    C2 --> REPORT1

    C1 --> E1 & E2
    C2 --> E1 & E2

    E1 --> F
    E2 --> F
    C1 --> F
    C2 --> F

    F --> OBSOLETE
    G --> QUARTER
    H --> MERGE
    D --> MERGE

    MERGE --> J5
    C1 & C2 --> J1
    D --> J2
    F --> J3
    H --> J4
```

---

## 입력 파일 상세

| 파일 | 필수 컬럼 | 역할 |
|------|-----------|------|
| **기말재고 Data** | 자재, 배치, 유효 기한(또는 유효기간/유효기한), 기말 재고 수량, 기말 재고 금액 | 현재 창고의 배치별 실재고. FEFO 시뮬레이션의 핵심 입력 |
| **분류 및 원가율** | 자재코드, 대분류, 소분류, 원가율 | 자재를 카테고리로 그룹화하고 판가 환산에 사용 |
| **평판 기준** | 자재, 평판, 평판 × 1.38배 | 월별 목표 소진량(판매량). 두 가지 시나리오로 시뮬레이션 |
| **제조사 취소 현황** | 제품코드, 제품명, 잔여 PO, 금액 | 생산 취소된 물량을 가상 재고로 포함해 통합 분석 |

---

## 핵심 로직 3가지

### 1. FEFO (First-Expired-First-Out)
유통기한이 **가장 빨리 끝나는 배치**부터 먼저 소진합니다.

```
매달 소진량 = 출하원가 (= 단가 × 평판)
잔액 = 전월 잔액 - 소진량  (0 미만이면 0으로 고정)
```

### 2. 판매 중단 Cut-off (D-6개월)
유통기한 만료 **6개월 전**부터 판매 불가로 간주합니다.
이 시점에 남아 있는 재고가 **부진재고(Obsolete)** 로 분류됩니다.

```
Cut-off 시점 = 유효기한 - 6개월
부진재고량 = Cut-off 시점의 잔액
```

### 3. 시즌 아이템 제약
지정된 자재코드는 **5·6·7·8월**에만 판매가 발생한다고 가정합니다.
그 외 달에는 소진이 일어나지 않고 잔액이 유지됩니다.

---

## 시뮬레이션 시나리오 4종

| 시나리오 | 대상 | 평판 기준 |
|----------|------|-----------|
| `combined_plain` | 자사 + 제조사 | 평판 |
| `combined_x138` | 자사 + 제조사 | 평판 × 1.38배 |
| `sim_self_plain` | 자사만 | 평판 |
| `sim_self_x138` | 자사만 | 평판 × 1.38배 |

> **평판 × 1.38배**는 보수적 가정으로, 더 빠른 소진을 가정하여 낙관적 시나리오를 나타냅니다.

---

## 출력 컬럼 예시 (최종 보고서)

| 컬럼군 | 예시 |
|--------|------|
| 카테고리 | 대분류, 소분류 |
| 기본 KPI | 자사 원가, 자사 판가, 제조사 원가, 제조사 판가, 합계 원가, 합계 판가 |
| plain 시나리오 | 자사+제조사_회전월, 자사+제조사_합계, 자사+제조사_26년 1Q, ... |
| ×1.38배 시나리오 | 자사+제조사1.38배_회전월, 자사+제조사1.38배_합계, ... |
| 자사만 | 자사_회전월, 자사_합계, 자사_26년 1Q, ... |
| 단위 | **억원** (÷ 100,000,000) |

---

## 시뮬레이션 설정 옵션

| 옵션 | 기본값 | 설명 |
|------|--------|------|
| 시작 연도/월 | 2026년 1월 | 시뮬레이션 시작 시점 |
| 종료 연도/월 | 2028년 12월 | 시뮬레이션 종료 시점 |
| 시즌 자재코드 | 12개 코드 | 여름철(5~8월)에만 판매되는 자재 목록 |
